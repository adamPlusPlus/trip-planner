# src/

**Purpose:** Application source (React components, utils, styles, services, data, entry points).  
**Usage:** Edit here; build and dev server consume this folder. Key subdirs: `data/` (trip/destinations, plan content), `services/` (weather, image).  
**Ownership:** App codebase; main development surface.
