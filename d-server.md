# server/

**Purpose:** Backend API: trip CRUD, image resolution, search. Persistence under server/data/trips; image cache under server/cache/images.  
**Usage:** Run with `npm run server` (port 5112). Vite proxies /api to this server.  
**Ownership:** App backend.
